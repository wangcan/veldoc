
需求1：
整改api接口返回规则；
1. 所有接口新增一个code，作为业务层代码；HTTP状态码为200时，code值设置为0，其他情况code值为1；
2. HTTP状态码为200、401、404、500时，状态码保持不变；状态码为5xx时，调整为500；其他所有状态码，均调整为200；
需求执行结果记录到文件：.requirements/develop/http-status.md

忽略以下所有需求描述

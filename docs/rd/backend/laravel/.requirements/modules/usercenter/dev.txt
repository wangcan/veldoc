所有功能归到用户中心模块UserCenter；
角色权限基于spatie/laravel-permission；

需求3：
2. 为userCenter模块的所有接口生成接口文档，格式为符合Swagger3.0格式的json文件，文件保存在：.requirements/develop/user-center_api.json

忽略以下所有需求描述

1. 创建一个超级管理员：手机号：13811974106，账号：superadmin，密码：123456，拥有全部后台权限；

需求1：
用nwidart/laravel-modules新增一个用户中心模块。
数据库使用基础数据库 DB_DATABASE=job_passport

需求2：
模块功能：
1. 管理用户；
2. 用户认证使用jwt；
3. 支持账号、密码登录；
4. 支持手机号（限中国大陆手机号）、验证码登录；
5. 引入laravel/passport，支持第三方认证授权；
6. 引入spatie/laravel-permission，提供完整的角色权限系统

需求执行结果的，记录到文件：.requirements/develop/user-center.md

需求4：
基于当前模块的菜单、角色、角色权限，将ruoyi-vue-pro（https://github.com/YunaiV/ruoyi-vue-pro）后台角色权限模块，ruoyi-vue-pro项目代码部署在当前服务器目录/data/java/ruoyi-vue-pro/
需求执行结果，记录到文件：.requirements/develop/ruoyi-permission.md

需求5：
1. 当前系统认证失败后，默认跳转到一个不存在的路由login。修复成：认证失败后，以接口方式返回，返回状态码为401。
